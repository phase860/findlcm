#include <vector>
#include <map>
#include <cstdint>


void mapToVector(std::map<uint64_t, uint64_t> &, 
        std::vector<uint64_t> &);

void showVector(std::vector<uint64_t> &); 

void showMap(std::map<uint64_t, uint64_t> &); 
